{
  "$defs": {
    "AppProjectResourcePattern": {
      "additionalProperties": false,
      "properties": {
        "group": {
          "description": "Kubernetes API group pattern, such as `apiextensions.k8s.io` or `*`.",
          "type": "string"
        },
        "kind": {
          "description": "Kubernetes resource kind pattern.",
          "type": "string"
        },
        "name": {
          "description": "Optional resource name pattern.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "group",
        "kind"
      ],
      "type": "object"
    },
    "AppProjectTemplate": {
      "additionalProperties": false,
      "description": "A constrained AppProject generated for one ApplicationGroup and target.",
      "properties": {
        "clusterResourceWhitelist": {
          "description": "Explicit cluster-scoped resource permissions. Namespace permissions are added for approved namespaces when creation is enabled.",
          "items": {
            "$ref": "#/$defs/AppProjectResourcePattern"
          },
          "type": "array"
        },
        "destinationNamespaces": {
          "description": "Permitted destination namespace patterns. Must cover all effective Release namespaces and additional namespaces; required when no fixed destination namespace is configured.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "name": {
          "description": "Generated AppProject name; defaults to the ApplicationGroup name. Must be unambiguous across targets sharing an Argo CD namespace.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "ApplicationDeletionPolicy": {
      "oneOf": [
        {
          "const": "Foreground",
          "description": "Cascade deletion in the foreground using the Argo CD resources finalizer.",
          "type": "string"
        },
        {
          "const": "Background",
          "description": "Cascade deletion in the background using the Argo CD background finalizer.",
          "type": "string"
        },
        {
          "const": "Orphan",
          "description": "Omit the finalizer and leave workload resources in place.",
          "type": "string"
        }
      ]
    },
    "ApplicationGroupSource": {
      "additionalProperties": false,
      "description": "A local source when no repository is given, otherwise an immutable remote source.",
      "oneOf": [
        {
          "not": {
            "anyOf": [
              {
                "properties": {
                  "repository": {
                    "not": {
                      "type": "null"
                    }
                  }
                },
                "required": [
                  "repository"
                ]
              }
            ]
          },
          "properties": {
            "repositoryRef": {
              "not": {
                "type": "null"
              }
            }
          },
          "required": [
            "repositoryRef"
          ]
        },
        {
          "not": {
            "anyOf": [
              {
                "properties": {
                  "repositoryRef": {
                    "not": {
                      "type": "null"
                    }
                  }
                },
                "required": [
                  "repositoryRef"
                ]
              }
            ]
          },
          "properties": {
            "repository": {
              "not": {
                "type": "null"
              }
            }
          },
          "required": [
            "repository"
          ]
        },
        {
          "properties": {
            "repository": {
              "type": "null"
            },
            "repositoryRef": {
              "type": "null"
            }
          }
        }
      ],
      "properties": {
        "commit": {
          "description": "Full immutable remote Git commit lock used for rendering.",
          "type": [
            "string",
            "null"
          ]
        },
        "exclude": {
          "description": "Relative file globs removed after inclusion.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "include": {
          "default": [
            "*.yaml",
            "*.yml"
          ],
          "description": "Relative candidate file globs. Only entries with a literal Release are rendered; attach other files using Release `spec.include`.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "path": {
          "description": "Normalized project-relative source directory; relative to the checkout for a remote source.",
          "type": "string"
        },
        "recursive": {
          "default": true,
          "description": "Whether source discovery descends into subdirectories.",
          "type": "boolean"
        },
        "rendererConfig": {
          "$ref": "#/$defs/RendererConfig",
          "default": {
            "mode": "Central"
          },
          "description": "Whether remote rendering uses central configuration or the remote project. Remote sessions cannot access secrets or the process environment."
        },
        "repository": {
          "anyOf": [
            {
              "$ref": "#/$defs/InlineGitRepository"
            },
            {
              "type": "null"
            }
          ],
          "description": "Inline remote Git coordinates. Mutually exclusive with `repositoryRef`; requires `revision` and `commit`."
        },
        "repositoryRef": {
          "anyOf": [
            {
              "$ref": "#/$defs/LocalReference"
            },
            {
              "type": "null"
            }
          ],
          "description": "GitRepository name for a remote source. Mutually exclusive with inline `repository`; requires `revision` and `commit`."
        },
        "revision": {
          "description": "Human-readable remote Git revision refreshed by `nyl update source-locks`.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "path"
      ],
      "type": "object"
    },
    "ApplicationGroupSpec": {
      "additionalProperties": false,
      "oneOf": [
        {
          "not": {
            "anyOf": [
              {
                "properties": {
                  "projectTemplate": {
                    "not": {
                      "type": "null"
                    }
                  }
                },
                "required": [
                  "projectTemplate"
                ]
              }
            ]
          },
          "properties": {
            "projectRef": {
              "not": {
                "type": "null"
              }
            }
          },
          "required": [
            "projectRef"
          ]
        },
        {
          "not": {
            "anyOf": [
              {
                "properties": {
                  "projectRef": {
                    "not": {
                      "type": "null"
                    }
                  }
                },
                "required": [
                  "projectRef"
                ]
              }
            ]
          },
          "properties": {
            "projectTemplate": {
              "not": {
                "type": "null"
              }
            }
          },
          "required": [
            "projectTemplate"
          ]
        }
      ],
      "properties": {
        "annotations": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Annotations added to generated Applications.",
          "type": "object"
        },
        "applicationDeletionPolicy": {
          "$ref": "#/$defs/ApplicationDeletionPolicy",
          "default": "Foreground",
          "description": "Argo CD Application finalizer policy: foreground cascade, background cascade, or orphan workloads."
        },
        "applicationNameTemplate": {
          "description": "Generated workload Application name template, with `release` context. Defaults to the Release name; use target-qualified templates when targets share an Argo CD namespace.",
          "type": [
            "string",
            "null"
          ]
        },
        "applicationNamespace": {
          "description": "Namespace containing generated Argo CD Applications.",
          "type": "string"
        },
        "destinationNamespace": {
          "description": "Workload namespace override. Defaults to each Release namespace.",
          "type": [
            "string",
            "null"
          ]
        },
        "enabled": {
          "default": true,
          "description": "Whether to render this selected group. Evaluated after target selection and may be templated.",
          "type": "boolean"
        },
        "labels": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Labels added to generated Applications.",
          "type": "object"
        },
        "namespace": {
          "$ref": "#/$defs/ManagedNamespacePolicy",
          "default": {
            "create": true,
            "deletePolicy": "Confirm",
            "prunePolicy": "Confirm"
          },
          "description": "Namespace creation and deletion policy for workload-owned namespaces."
        },
        "outputPath": {
          "description": "Relative output directory beneath the target prefix. Defaults to the group name.",
          "type": [
            "string",
            "null"
          ]
        },
        "projectRef": {
          "description": "Project-local AppProjectDefinition identity. Exactly one of `projectRef` and `projectTemplate` is required.",
          "type": [
            "string",
            "null"
          ]
        },
        "projectTemplate": {
          "anyOf": [
            {
              "$ref": "#/$defs/AppProjectTemplate"
            },
            {
              "type": "null"
            }
          ],
          "description": "Constrained generated AppProject. Exactly one of `projectRef` and `projectTemplate` is required."
        },
        "releaseCustomization": {
          "$ref": "#/$defs/GitOpsReleaseCustomization",
          "default": {},
          "description": "Limits on per-release Application overrides. Releases cannot expand the generated project or namespace policy."
        },
        "sharedNamespaces": {
          "additionalProperties": {
            "$ref": "#/$defs/SharedNamespacePolicy"
          },
          "description": "Explicit ownership by namespace name. Every consuming group must agree. Kubernetes bootstrap namespaces are externally owned unless explicitly delegated here.",
          "type": "object"
        },
        "source": {
          "anyOf": [
            {
              "$ref": "#/$defs/ApplicationGroupSource"
            },
            {
              "type": "null"
            }
          ],
          "description": "Release source selection. Omission derives `applications/<group-name>` for central groups or the containing directory for `_application-group.yaml`."
        },
        "syncPolicy": {
          "anyOf": [
            {
              "$ref": "#/$defs/GitOpsSyncPolicy"
            },
            {
              "type": "null"
            }
          ],
          "description": "Workload Application sync policy. Generated Applications include apply-only-out-of-sync and server-side apply unless explicitly overridden."
        }
      },
      "required": [
        "applicationNamespace"
      ],
      "type": "object"
    },
    "GitOpsAutomatedSyncPolicy": {
      "additionalProperties": false,
      "properties": {
        "enabled": {
          "description": "Whether automated sync is enabled. Omission enables it when the automated block is present.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "prune": {
          "default": false,
          "description": "Allow Argo CD to prune resources removed from desired manifests.",
          "type": "boolean"
        },
        "selfHeal": {
          "default": false,
          "description": "Allow Argo CD to repair live drift automatically.",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "GitOpsReleaseCustomization": {
      "additionalProperties": false,
      "properties": {
        "allowedPaths": {
          "description": "Dotted field globs admitted in Release Application overrides. `*` matches one segment; `**` matches multiple segments. Core ownership fields remain protected.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "allowedSyncOptions": {
          "description": "Exact allowed values for Release `spec.syncPolicy.+syncOptions` overrides. Same-key values replace the generated option.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "deniedPaths": {
          "description": "Dotted field globs forbidden in Release Application overrides. Deny takes precedence over allow.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "GitOpsResourceMetadata": {
      "additionalProperties": false,
      "description": "Metadata shared by local GitOps compiler resources.",
      "properties": {
        "labels": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Literal labels used for organization and target selection.",
          "type": "object"
        },
        "name": {
          "description": "Project-local identity. Must be a Kubernetes DNS subdomain and remain static during discovery.",
          "type": "string"
        }
      },
      "required": [
        "name"
      ],
      "type": "object"
    },
    "GitOpsSyncPolicy": {
      "additionalProperties": false,
      "properties": {
        "automated": {
          "anyOf": [
            {
              "$ref": "#/$defs/GitOpsAutomatedSyncPolicy"
            },
            {
              "type": "null"
            }
          ],
          "description": "Presence enables automated synchronization unless `enabled` is false."
        },
        "syncOptions": {
          "description": "Argo CD sync options. Generated Applications add `ApplyOutOfSyncOnly=true` and `ServerSideApply=true` unless an option with the same key is supplied.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "InlineGitRepository": {
      "additionalProperties": false,
      "properties": {
        "publishURL": {
          "description": "Optional Git write URL. Publication uses `repoURL` when omitted.",
          "type": [
            "string",
            "null"
          ]
        },
        "repoURL": {
          "description": "Credential-free Git read URL used by renderers and generated Applications.",
          "type": "string"
        }
      },
      "required": [
        "repoURL"
      ],
      "type": "object"
    },
    "LocalReference": {
      "additionalProperties": false,
      "properties": {
        "name": {
          "description": "Project-local name of the referenced resource; its kind and API group are determined by the containing field.",
          "type": "string"
        }
      },
      "required": [
        "name"
      ],
      "type": "object"
    },
    "ManagedNamespacePolicy": {
      "additionalProperties": false,
      "properties": {
        "create": {
          "default": true,
          "description": "Synthesize missing destination and additional Namespaces within the owning workload Application.",
          "type": "boolean"
        },
        "deletePolicy": {
          "$ref": "#/$defs/ManagedResourceDeletionPolicy",
          "default": "Confirm",
          "description": "Namespace deletion policy when the owning Application is deleted."
        },
        "prunePolicy": {
          "$ref": "#/$defs/ManagedResourceDeletionPolicy",
          "default": "Confirm",
          "description": "Namespace pruning policy: Automatic, Confirm, or Retain."
        }
      },
      "type": "object"
    },
    "ManagedResourceDeletionPolicy": {
      "oneOf": [
        {
          "const": "Automatic",
          "description": "Allow pruning or deletion without additional restrictions.",
          "type": "string"
        },
        {
          "const": "Confirm",
          "description": "Require confirmation through the Argo CD Prune or Delete sync option.",
          "type": "string"
        },
        {
          "const": "Retain",
          "description": "Set the Argo CD Prune or Delete sync option to false.",
          "type": "string"
        }
      ]
    },
    "RendererConfig": {
      "additionalProperties": false,
      "properties": {
        "mode": {
          "$ref": "#/$defs/RendererConfigMode",
          "default": "Central",
          "description": "Central uses platform configuration; Remote loads the remote project and requires a remote source."
        },
        "projectPath": {
          "description": "Remote project root, relative to the checkout. Defaults to `.`; valid only in Remote mode and must remain inside the checkout.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "RendererConfigMode": {
      "oneOf": [
        {
          "const": "Central",
          "description": "Use the central platform project configuration.",
          "type": "string"
        },
        {
          "const": "Remote",
          "description": "Load project configuration from the remote source checkout with restricted inputs.",
          "type": "string"
        }
      ]
    },
    "SharedNamespaceOwner": {
      "oneOf": [
        {
          "additionalProperties": false,
          "description": "The selected workload Release owns the Namespace; other workloads must not render its Namespace object.",
          "properties": {
            "applicationGroup": {
              "description": "ApplicationGroup owning the Namespace Application or selected workload Release.",
              "type": "string"
            },
            "kind": {
              "const": "Release",
              "type": "string"
            },
            "release": {
              "description": "Release name within the owning ApplicationGroup.",
              "type": "string"
            }
          },
          "required": [
            "kind",
            "applicationGroup",
            "release"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "Generate a dedicated Namespace Application under the selected group; workloads must not render this Namespace.",
          "properties": {
            "applicationGroup": {
              "description": "ApplicationGroup owning the Namespace Application or selected workload Release.",
              "type": "string"
            },
            "kind": {
              "const": "Dedicated",
              "type": "string"
            }
          },
          "required": [
            "kind",
            "applicationGroup"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "The Namespace is externally owned. Nyl neither synthesizes nor accepts an authored Namespace object.",
          "properties": {
            "kind": {
              "const": "External",
              "type": "string"
            }
          },
          "required": [
            "kind"
          ],
          "type": "object"
        }
      ]
    },
    "SharedNamespacePolicy": {
      "additionalProperties": false,
      "description": "Explicit authorization and ownership for a namespace consumed by more than\none generated workload Application.",
      "properties": {
        "owner": {
          "$ref": "#/$defs/SharedNamespaceOwner",
          "description": "The sole owner of a shared Namespace. Release and Dedicated owners identify the responsible ApplicationGroup."
        }
      },
      "required": [
        "owner"
      ],
      "type": "object"
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "Selects Kubernetes Releases and defines their Argo CD Application, project, and namespace policies.\n\nLiteral metadata labels participate in target selection; the spec is rendered afterward. This is compiler configuration, not a workload manifest.\n\n## When needed\n\nRequired to select Release entry files and generate their workload Applications in rendered GitOps.\n\n## If omitted\n\nNyl does not infer groups from directories or Release files. Without a selected, enabled ApplicationGroup, a target emits no workload Applications. Direct file rendering does not require an ApplicationGroup.",
  "examples": [
    {
      "apiVersion": "k8s.gitops.nyl/v1",
      "kind": "ApplicationGroup",
      "metadata": {
        "name": "workloads"
      },
      "spec": {
        "applicationNamespace": "argocd",
        "destinationNamespace": "workloads",
        "projectTemplate": {
          "destinationNamespaces": [
            "workloads"
          ]
        },
        "source": {
          "path": "applications/workloads"
        }
      }
    }
  ],
  "properties": {
    "apiVersion": {
      "const": "k8s.gitops.nyl/v1",
      "description": "API group and version defining this resource.",
      "type": "string"
    },
    "kind": {
      "const": "ApplicationGroup",
      "description": "Resource kind within the API group.",
      "type": "string"
    },
    "metadata": {
      "$ref": "#/$defs/GitOpsResourceMetadata",
      "description": "Resource identity and metadata."
    },
    "spec": {
      "$ref": "#/$defs/ApplicationGroupSpec",
      "description": "Configuration for this resource."
    }
  },
  "required": [
    "apiVersion",
    "kind",
    "metadata",
    "spec"
  ],
  "title": "ApplicationGroup",
  "type": "object"
}
