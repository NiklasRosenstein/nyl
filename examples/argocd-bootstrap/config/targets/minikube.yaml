apiVersion: gitops.nyl/v1
kind: DeploymentTarget
metadata:
  name: minikube
spec:
  clusterRef:
    name: minikube
  publication:
    repository:
      repoURL: https://example.invalid/nyl-argocd-bootstrap-deploy.git
    revision: deploy/minikube
